# Keyless ACP compatibility fixture with ordinary sandbox policy.
- id: llm-deepseek
  disabled: true
- id: llm-pi-ai
  disabled: true
- id: plugin-package-inventory-deepseek
  disabled: true
- id: session-persistence-jsonl
  config:
    root: !!js process.env.DSH_SNAPSHOT_SESSIONS_ROOT
    compression: none
- insert:
    - id: llm-replay
      name: '@deepseek-ai/dsh-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
