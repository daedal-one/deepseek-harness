/**
 * OpenRouter key spend and per-session USD cost estimates, served to the Web
 * UI as the Remote-only `openrouterSpend` service.
 * @module @deepseek-ai/dsh-openrouter-spend
 */

export { default, Config, OpenRouterSpendService } from './service.ts'
export type * from './types.ts'
