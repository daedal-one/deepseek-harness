# Changelog

## 0.1.2

Add opt-in confidential execution and a functional probe requiring Landlock ABI 3 filesystem rights plus mandatory seccomp denial of new network endpoints and cross-process descriptor/memory access. Existing grant flags and partial/full probe behavior remain unchanged.
